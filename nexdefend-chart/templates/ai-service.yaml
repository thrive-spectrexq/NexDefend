apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.ai.name }}
  labels:
    app.kubernetes.io/name: {{ .Values.ai.name }}
    app.kubernetes.io/instance: {{ .Release.Name }}
spec:
  type: {{ .Values.ai.service.type }}
  ports:
    - port: {{ .Values.ai.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ .Values.ai.name }}
    app.kubernetes.io/instance: {{ .Release.Name }}
